import { redirectRetiredSchoolRoute } from "@/lib/school-retired";

/** School purchasing removed — supplier owns supply. */
export default function SchoolOrdersPage() {
  redirectRetiredSchoolRoute();
}
