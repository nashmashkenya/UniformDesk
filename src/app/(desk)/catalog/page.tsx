import { redirectRetiredSchoolRoute } from "@/lib/school-retired";

/** Catalog owned by supplier; school focuses on stock + issue + reports. */
export default function CatalogPage() {
  redirectRetiredSchoolRoute();
}
